# Railpack (défaut Railway) ne supporte pas Bun : build explicite via Dockerfile.
[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "bun run start"
preDeployCommand = "bun run db:migrate && bun run deploy-commands"
restartPolicyType = "ON_FAILURE"
